apiVersion: v1
kind: Service
metadata:
  name: golang-observ-svc
  labels:
    app: golang-observ-svc
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: golang-observ-svc



